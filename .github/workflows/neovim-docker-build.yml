name: Akatsuki Docker Build

on:
  pull_request:
    types: [opened, synchronize, reopened]
    paths:
      - 'neovim-docker/**'
      - '.github/workflows/neovim-docker-build.yml'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-docker-images:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: true
      matrix:
        include:
          - name: neovim-docker-default-arm
            dockerfile: neovim-docker/neovim-docker-default-arm/Dockerfile
            context: neovim-docker/neovim-docker-default-arm
            platform: linux/arm64

          - name: neovim-docker-default-amd
            dockerfile: neovim-docker/neovim-docker-default-amd/Dockerfile
            context: neovim-docker/neovim-docker-default-amd
            platform: linux/amd64

          - name: neovim-docker-python
            dockerfile: neovim-docker/neovim-docker-python/Dockerfile
            context: neovim-docker/neovim-docker-python
            platform: linux/arm64

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image
        uses: docker/build-push-action@v6
        with:
          context: ${{ matrix.context }}
          file: ${{ matrix.dockerfile }}
          platforms: ${{ matrix.platform }}
          push: false
          load: true
          tags: ${{ matrix.name }}:pr-${{ github.event.pull_request.number }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
